<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wxy.exoskeleton.mapper.DetailedInfoMapper">
        <resultMap id="BaseResultMap" type="com.wxy.exoskeleton.model.DetailedInfo">
            <id column="id" property="id" jdbcType="INTEGER"/>
            <result column="uid" property="uid" jdbcType="INTEGER"/>
            <result column="number" property="number" jdbcType="VARCHAR"/>
            <result column="address" property="address" jdbcType="VARCHAR"/>
            <result column="date" property="date" jdbcType="DATE"/>
        </resultMap>

    <insert id="insertDInfoSelective" parameterType="com.wxy.exoskeleton.model.AllInfo">
        insert into detailedmsg (uid, number, address, date)
        values
        (#{id,jdbcType=INTEGER}, #{number,jdbcType=VARCHAR}, #{address,jdbcType=VARCHAR}, #{date,jdbcType=DATE})
    </insert>

    <select id="getDInfoByUid" resultMap="BaseResultMap">
        select
            id, uid, number, address, date
        from detailedmsg
        where uid = #{uid,jdbcType=INTEGER}
    </select>

    <select id="getAInfoByUid" resultType="com.wxy.exoskeleton.model.AllInfo">
        select
            info.id,NAME,gender,age,weight,height,legLength,crotchWidth,isPatient,TYPE,uid,NUMBER,address,DATE
        from info,detailedmsg
        where info.id = detailedmsg.uid AND uid = #{uid,jdbcType=INTEGER}
    </select>

    <update id="updateDInfoByPrimaryKeySelective" parameterType="com.wxy.exoskeleton.model.DetailedInfo">
        update allinfo
        <set>
            <if test="number != null">
                number = #{number,jdbcType=VARCHAR},
            </if>
            <if test="address != null">
                address = #{address,jdbcType=VARCHAR},
            </if>
            <if test="date != null">
                date = #{date,jdbcType=DATE},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <delete id="resetDInfo" parameterType="java.lang.Integer">
        update allinfo
        <set>
            number = NULL,
            address = NULL
        </set>
        where id = #{id,jdbcType=INTEGER}
    </delete>

    <insert id="addInfos">
        insert into detailedmsg (uid, number, address, date)
        values
        <foreach collection="list" separator="," item="allInfo">
            (#{allInfo.id,jdbcType=INTEGER}, #{allInfo.number,jdbcType=VARCHAR}, #{allInfo.address,jdbcType=VARCHAR}, #{allInfo.date,jdbcType=DATE})
        </foreach>
    </insert>

</mapper>